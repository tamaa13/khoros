// Shared motion vocabulary — one ease so every entrance, hover and swap reads
// as one hand (same curve as arca/dashboard).
export const EASE: [number, number, number, number] = [0.22, 1, 0.36, 1];
