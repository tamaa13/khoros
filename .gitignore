node_modules/
*.log

# QVAC model cache + generated audio artifacts
.qvac/
models/cache/
day0/artifacts/
*.wav

# Persisted agent memory (per-user, not for the repo)
data/

# Build artifacts (compiled binaries, packaged apps)
dist/
out/

# OS / editor
.DS_Store
